#!/usr/bin/python3
{{ ansible_managed }}
from flask import Flask

app = Flask(__name__)

@app.route('/')
def index():
    return 'Hello world from the app server: {{ inventory_hostname }}'

if __name__ == "__main__":
    app.run(host='{{ ansible_default_ipv4.address }}', port=8888)
